name: Backend CI

on:
  push:
    branches: [ main, master ]
    paths:
      - 'backend/**'
      - '.github/workflows/ci-backend.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'backend/**'
      - '.github/workflows/ci-backend.yml'

jobs:
  lint:
    name: Lint Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: backend/go.sum
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: backend
          args: --timeout=5m

  sast:
    name: Security Scan (SAST)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Gosec Security Scanner
        uses: securego/gosec@master
        with:
          args: ./backend/...

  test:
    name: Test and Smoke
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          # Vetor 5: senha vem exclusivamente do GitHub Secret — sem fallback hardcoded.
          # Configure em: Settings → Secrets and variables → Actions → POSTGRES_PASSWORD
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: seguros_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: backend/go.sum

      - name: Run Unit Tests
        working-directory: ./backend
        run: go test -race ./...

      - name: Run Migrations and Seed
        working-directory: ./backend
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          # Vetor 5: variável de ambiente injetada do secret — nunca do repositório.
          PG_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          PG_DBNAME: seguros_db
          PG_SSLMODE: disable
        run: go run ./cmd/migrate -seed

      - name: Run Smoke Integration Tests
        working-directory: ./backend
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          PG_DBNAME: seguros_db
          PG_SSLMODE: disable
          # Vetor 5: JWT_SECRET vem do secret — sem string vazia ou valor hardcoded.
          # Use uma string aleatória de 32+ chars em: Settings → Secrets → JWT_SECRET
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          # GEMINI_API_KEY não é necessária nos testes de smoke (endpoint desabilitado com chave vazia).
          # Se quiser testar com IA em CI, adicione o secret GEMINI_API_KEY no GitHub.
        run: |
          go run ./cmd/api &
          sleep 5
          go test -tags smoke ./smoke_test.go -v
